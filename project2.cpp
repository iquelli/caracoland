#include <iostream>
#include <vector>
#include <unordered_map>
#include <cstring>
#include <string>
using namespace std;

int vertices;
int arcs;

//TODO finish

int main() {
    scanf("%d", &vertices);
    scanf("%d", &arcs);

    for(int i = 0; i < arcs; i++) {
        scanf("%d %d %d");
    }

    return 0;
}